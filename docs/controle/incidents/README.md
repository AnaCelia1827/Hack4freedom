# Incidentes

Registros append-only de falhas observadas após execução. Use o template em
`../templates/incident.json`. Nunca trate rollback financeiro como remoção de
histórico: use reconciliação, compensação ou refund explícito.
